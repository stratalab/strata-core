{
  "$defs": {
    "GraphAnalyticsBudget": {
      "additionalProperties": false,
      "description": "Optional size bounds for one graph analytics snapshot (input form).\nUnset fields use the engine defaults.",
      "properties": {
        "max_edges": {
          "description": "Maximum edge count admitted into the snapshot.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_nodes": {
          "description": "Maximum node count admitted into the snapshot.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GraphDirection": {
      "description": "Graph neighbor traversal direction.",
      "oneOf": [
        {
          "const": "outgoing",
          "description": "Outgoing edges from the selected node.",
          "type": "string"
        },
        {
          "const": "incoming",
          "description": "Incoming edges into the selected node.",
          "type": "string"
        },
        {
          "const": "both",
          "description": "Incoming and outgoing edges.",
          "type": "string"
        }
      ]
    },
    "GraphSsspData": {
      "description": "Shortest-path result (wire form). Unreachable nodes are omitted.",
      "properties": {
        "direction": {
          "$ref": "#/$defs/GraphDirection"
        },
        "distances": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "type": "object"
        },
        "graph": {
          "type": "string"
        },
        "predecessors": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "The node each reachable node's cheapest walk arrived from; the source\nhas no entry. Following it back to the source unpacks the path.",
          "type": "object"
        },
        "source": {
          "type": "string"
        }
      },
      "required": [
        "graph",
        "source",
        "direction",
        "distances",
        "predecessors"
      ],
      "type": "object"
    }
  },
  "$id": "https://stratadb.org/idl/v1/schemas/graph.analytics.sssp.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "command": "graph.analytics.sssp",
  "request": {
    "additionalProperties": false,
    "description": "Computes shortest-path distances and predecessors from a source node.\n\n# Guaranteed semantics\n\n- **Cheapest walk, not a route.** Distances are Dijkstra over edge\n  weights on one consistent snapshot; `predecessors` gives, for every\n  reachable node but the source, the node its cheapest walk arrived\n  from, so a path unpacks by following it back to the source. Under\n  `both`, a step may run against an edge's stored direction; the result\n  is a node sequence, not a legal drive (turn restrictions are not\n  modelled).\n- **Ties are deterministic.** Two equal-cost walks resolve to the one\n  discovered first: the frontier pops by (distance, node id order),\n  edges relax in (edge type, neighbor) order, and an equal-cost\n  alternative never replaces a recorded predecessor — the same\n  snapshot always yields the same predecessors.\n- **Edge-type filter.** `edge_types` restricts every relaxation to the\n  listed types; absent, every type is walked. A type the graph does\n  not contain restricts to nothing rather than failing — the same rule\n  as `bfs`.\n- **Negative weights refuse for the edges the walk may use.** A\n  negative edge of a selected type — any type, when unrestricted —\n  refuses with `failed_precondition.engine.graph_negative_weight`\n  before any work; a negative edge of an excluded type does not.",
    "properties": {
      "as_of": {
        "description": "Read as of a position on the logical commit timeline — the\n`timestamp` from `history` output, not the `version`, and never a\ncalendar date. Reads the graph state visible at that timeline\nposition. To read as of a real time, use `as_of_time` instead.",
        "format": "uint64",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      },
      "as_of_time": {
        "description": "Read as of a real time: a wall-clock instant in microseconds since\nthe Unix epoch (UTC), as reported by `committed_at` on a write ack\nor on any `history` row. Resolves to the commit at or before that\ninstant, and fails rather than guessing if the instant falls\noutside the branch's recorded history. Mutually exclusive with\n`as_of`.",
        "format": "uint64",
        "minimum": 0,
        "type": [
          "integer",
          "null"
        ]
      },
      "branch": {
        "description": "Target branch. Defaults to the executor handle branch.",
        "type": [
          "string",
          "null"
        ]
      },
      "budget": {
        "anyOf": [
          {
            "$ref": "#/$defs/GraphAnalyticsBudget"
          },
          {
            "type": "null"
          }
        ],
        "description": "Optional snapshot size bounds. Defaults to the engine limits."
      },
      "direction": {
        "anyOf": [
          {
            "$ref": "#/$defs/GraphDirection"
          },
          {
            "type": "null"
          }
        ],
        "description": "Optional traversal direction. Defaults to outgoing."
      },
      "edge_types": {
        "description": "Optional edge-type restriction applied at every relaxation.",
        "items": {
          "type": "string"
        },
        "type": [
          "array",
          "null"
        ]
      },
      "graph": {
        "description": "Graph name.",
        "type": "string"
      },
      "source": {
        "description": "Source node id.",
        "type": "string"
      },
      "space": {
        "description": "Target product space. Defaults to `\"default\"`.",
        "type": [
          "string",
          "null"
        ]
      },
      "type": {
        "const": "graph_sssp",
        "type": "string"
      }
    },
    "required": [
      "type",
      "graph",
      "source"
    ],
    "type": "object"
  },
  "response": {
    "description": "Shortest-path distances computed over a graph snapshot.",
    "properties": {
      "data": {
        "$ref": "#/$defs/GraphSsspData"
      },
      "type": {
        "const": "graph_sssp_result",
        "type": "string"
      }
    },
    "required": [
      "type",
      "data"
    ],
    "type": "object"
  }
}
